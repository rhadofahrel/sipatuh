{
  "$schema": "https://railpack.com/schema.json",
  "steps": {
    "extensions": {
      "commands": [
        "install-php-extensions ctype curl dom fileinfo filter hash mbstring openssl pcre pdo session tokenizer xml gd"
      ]
    }
  }
}